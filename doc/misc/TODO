diploma2:

- add missing license headers
- bug: document url is not accessible after completing the first human task?

Regularly:

- externalize strings
- e.printStackTrace(); -> log.severe(ExceptionUtils.getFullStackTrace(e));
